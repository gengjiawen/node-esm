version: 2
jobs:
  test:
    machine: true
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: test esm
          command: |
            source ~/.bashrc
            nvm i 11.3.0
            node --version
            node --experimental-modules test-esm.mjs
            node --experimental-modules .\test-esm.mjs

workflows:
  version: 2
  perfs:
    jobs:
      - test
